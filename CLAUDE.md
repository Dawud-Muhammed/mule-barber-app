You're working in mule-barber-app, an existing repo. Already scaffolded:Next.js 16.3.6 (App Router) + TypeScript + Tailwind CSS; @supabase/supabase-jsand @supabase/ssr installed; a real Supabase project (creds in .env.local,gitignored); a real Telegram bot created via BotFather (token in .env.local,not yet wired up); GitHub repo Dawud-Muhammed/mule-barber-app on main, initialscaffold pushed; `npm run dev` already works.Building: V1 of a queue & booking system for a real single-chair localbarbershop, "Mule Barber". Problem: clients wait around for hours not knowingtheir turn. Two surfaces: a Telegram bot (clients) and a web admin dashboard(owner). Core loop: client opens the bot → picks a service → joins the queue→ gets a queue number + count ahead → can check position anytime → getsauto-notified at "3 or fewer ahead" and again at "you're next" → owner runsthe queue from a live dashboard (now-serving + waiting list, complete / skip /cancel). No-shows get skipped, not stuck.Non-goals for V1: multi-barber, multi-location, payments, customer accounts,analytics, per-client timezones. One barber, one queue, one chair.Engineering rules — do not violate these:1. The live queue is always scoped to queue_date = current_date (Postgres   date; project timezone is UTC and that is fine for V1 since the shop and   its clients share one local day). Never query queue rows without the date   filter unless the code explicitly says "history".2. "One active entry per client per day" and "queue is open" are enforced by   the database (unique partial index, shop_state table), never only by app   code. App code checks first for good error messages; the DB is the   backstop.3. Migrations are append-only: never edit a migration that has been applied.   Schema change = new migration file.4. TypeScript strict, real error handling, no premature abstraction, no   dependencies you can't justify.5. Stack choices are locked — don't re-litigate them: grammy for the bot,   Supabase Auth (single admin) for the dashboard, Supabase Realtime for   live sync, Postgres RPC functions for anything that must be atomic,   Tailwind + shadcn/ui for styling.Design bar: this dashboard runs all day on a device at the front counter —it needs to look like something a design-award-winning engineer shipped, nota scaffold. Clean typography, real visual hierarchy, calm palette.Work in phases. Each phase below is a separate prompt with its own pass/failchecklist — implement only that phase, then stop and report what changed,which checklist items you verified and how, and any item you could notverify. Never mark your own work as passing.